# Mastermind — root / shared env example
# Copy to .env and set values. Do not commit .env.

# Control Plane (see control-plane/.env.example for full list)
DATABASE_URL=postgresql://mastermind:changeme@localhost:5432/mastermind
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET=change-me-user-secret
JWT_AGENT_SECRET=change-me-agent-secret
OPENAI_KEY_ENCRYPTION_SECRET=replace-with-a-long-random-secret

# Web (optional overrides)
NEXT_PUBLIC_CONTROL_PLANE_URL=http://localhost:3001

# Agent (see agent/config.yaml.example)
# CONTROL_PLANE_URL=http://localhost:3001
# PAIRING_TOKEN=
